{
  "name": "tschirlau-game",
  "scripts": {
    "dev": "bun --hot index.html",
    "build": "bun build.ts",
    "serve": "bun serve.ts",
    "package": "bun build.ts && bun build --compile package.ts --outfile tschirlau",
    "typecheck": "tsc --noEmit"
  },
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  }
}
